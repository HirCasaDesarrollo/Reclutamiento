@model IEnumerable<Prueba_BackEnd_Jr.Models.Ajuste>
@{
    ViewData["Title"] = "Requerimiento 3";
}

<div>
    <h2>Requerimiento 3</h2>
    <p class="fst-italic">"Una vez obtenidos estos datos, hacer un cruce de información con la tabla Ajuste para tener los totales reales colocándolo en la columna de Adeudo tomando en cuenta el MontoPagado de los pagos de los clientes y el MontoSolicitud."</p>
    <p>Presion el boton de "Solucionar requerimiento" para aplicar la solucion.</p>
    <p class="bg-info fst-italic p-2 m-1 text-center">@ViewData["mensaje"]</p>

    <div class="row mt-4">
        <div class="col-12 col-md-6">
            <h3>Tabla Ajustes (con los nombres de los clientes)</h3>
        </div>
        <div class="col-12 col-md-6 text-center text-md-end">
            <a class="text-center text-md-end btn btn-primary" asp-area="" asp-controller="Home" asp-action="Prueba3Solucion">Solucionar requerimiento</a>
        </div>
    </div>

    <table class="table text-center">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Cliente.Nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MontoTotal)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Adeudo)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var c in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => c.Cliente.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.MontoTotal)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.Adeudo)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>